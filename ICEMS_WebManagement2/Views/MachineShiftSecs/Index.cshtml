@model IEnumerable<ICEMS_WebManagement2.MachineShiftSecs>

@{
    ViewBag.Title = "Index";
}

<h2>Index MachineShiftSecs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.machineid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.shiftsecs)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.machineid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shiftsecs)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
